ROOT_PATH := $(call my-dir)/../../..
LOCAL_PATH := $(ROOT_PATH)/libx264
EXTERNAL := $(ROOT_PATH)/..
include $(CLEAR_VARS)

LOCAL_ARM_MODE := arm

TARGET_ARCH := arm
include $(LOCAL_PATH)/x264.mk

LOCAL_SRC_FILES := $(X264FILES)

LOCAL_C_INCLUDES :=		\
	$(LOCAL_PATH)

LOCAL_CFLAGS += $(X264CFLAGS)

LOCAL_SHARED_LIBRARIES := 
LOCAL_STATIC_LIBRARIES := $(X264LIBS)

LOCAL_LDLIBS := -llog

LOCAL_MODULE := $(X264NAME)

include $(BUILD_SHARED_LIBRARY)
